# Market Data Service Configuration

# Server port
PORT=4001

# SQLite database path
SQLITE_PATH=./data/market-data.db

# Log level: debug | info | warn | error
LOG_LEVEL=info

# ============================================================================
# DEPRECATED CONFIGURATION (Now managed via data/config.json)
# ============================================================================
# The following environment variables are deprecated and will be ignored.
# Provider, symbols, intervals, and backfill configuration is now managed
# via the data/config.json file and can be updated at runtime via the API.
#
# PROVIDER=mock
# SYMBOLS=BTC/USDT,ETH/USDT
# BACKFILL_HOURS=24
#
# See data/config.json for multi-provider configuration.
# Use the Configuration API (PUT /internal/config) to update at runtime.
